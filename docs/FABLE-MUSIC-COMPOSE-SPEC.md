# FABLE SPEC — the alternative Music surface ("Compose", Debi's ruling 2026-08-29)

**The ruling:** a REDESIGNED Music surface behind its OWN new button; the current Music view
stays untouched; Debi decides later which survives (or keeps both). Same pattern as Studio
(new axis, old intact) and Goose UI/CLI (two lanes coexist).

**BINDING sources:** docs/research/2026-08-29-surface-audit.md (the Music deep-dive: at-rest
inventory ~55 elements/~14 standing sentences; essential verbs; the field paragraph — Suno's
one Simple/Custom gate with Lyrics as the only promoted field, Udio's single Advanced dropdown)
+ docs/research/2026-08-29-generate-page-redesign.md §3/§4 (the house IA: at-rest/one-tap/
buried, sentence rule, chip grammar) + doctrine 8b (Suno et al are data points; our principles
govern).

## The IA
- **AT REST (≤7):** status chip(s) (engine/worker state, one verdict object) · a PLAYABLE HERO
  STAGE (the latest track, art/wave + play — the audit's biggest structural finding: today
  there is no result stage at all) · recent-tracks strip ("All tracks ▸" expands in place,
  mirroring Debi's Generate fork answer) · prompt box · lyrics as THE one promoted field
  (Suno-corroborated; collapsed placeholder when empty) · length control · engine chip ·
  **Generate**. (Count the composer cluster honestly; stay ≤7 standing groups.)
- **ONE TAP:** engines sheet in the v1.5.34 row/chip grammar (engine state chips, measured
  peaks as chips with provenance hovers — RAM advisory per the fit engine, never blocking);
  track detail (real name editing — raw filenames are not identity; format/seed/steps of THE
  run written back Pixelmator-style); More ▸ for steps/seed/format.
- **BURIED:** Help owns the essays — write the new USER-EXPLAINERS §Music the audit calls for;
  the page never duplicates it. Output paths/architecture jargon ("one-shot subprocess") never
  ship at rest. Sentence rule + the at-rest gate test, same as the Generate rebuild.
- Toasts never alert() (v1.5.39 rule). ALL-DESIGNS six looks by computed values. Copy
  provenance. Spawn-gates stay advisory (S4 retrofit rides separately).

## Wiring
Own NEW page (bridge/panel/compose.html or better name) reusing the EXISTING music backend
routes unchanged (panel-only slice unless a real gap appears — any router addition follows
the facade manifest). Nav: sidebar Workspace entry directly after Music, label **"Compose"**
(one-line rename later if Debi prefers another name), glyph unique (not ♪ if Music wears it —
follow the v1.5.38 ❖ lesson: never wear another control's glyph), tab declared-not-pinned,
fences widened with arguments per the goose/comfy precedent. Current Music view: ZERO changes.

---

# SPEC v2 — Compose visual rebuild (Debi's GO, 2026-08-29): "Spectrum" (compose-i)

BINDING design source: docs/mockups/2026-08-29/compose-i.html + visual-craft.md §3c.
Structure (Debi's spec, round 3): draggable block grid (~3/row, 2-3 rows, live reflow,
arrangement persisted), settings pane dockable left/right + shrinkable to half-height
(spill slot accepts blocks), ledger as ONE compact block, prompt + lyrics side by side
growing DOWNWARD with Compose on the line, quiet dock strip at the bottom with one ghost
slot (P6: stem separation is its first future occupant — nothing built for it yet).
THE IDENTITY: colour budget spent ONLY on the section-coloured waveform (hue as
information — the STRUCTURE data drives it; where real section data doesn't exist,
derive honestly from audio analysis or energy envelope, or fall back to a single-hue
ramp — NEVER paint fake sections). THE REAL-DATA RULE (Debi at GO): mockup depiction
data does not ship; every value is live or absent. All v1.5.43 conservation (30
mechanics, sentence rule, advisory gates, never-rename-files) carries forward.
