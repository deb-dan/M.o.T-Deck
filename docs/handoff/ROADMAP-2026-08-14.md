# Harness roadmap — everything remaining (2026-08-14, Fable 5)

Ordered by lane. ✋ = needs a Debi action · 🧠 = needs a Fable spec/QA first · 🔨 = ready to
delegate to a builder · 🔬 = research first.

## 1. Verify queue (Debi)
DONE 2026-08-15 — kept for the record, not for action:
- ✅ All three update passes shipped AND Mac-verified: MLX pair (mlx-vlm 0.6.13 /
  mlx-audio 0.4.8 — speak, dictation, conv, chat all fine), Hermes v2026.8.13
  (approval card + file write end-to-end, footer reads v0.20.1), llama.cpp b10427.
- ✅ Toolset lever, drag-a-tab, split v2, per-skill trimming, clarify cards all
  verified in the app. Contract gate now runs on Debi's Mac (105 passed locally).

STILL OPEN:
- ✋ Auto-reload check: `log stream --predicate 'process == "Harness"' | grep hermes`,
  toggle a toolset, switch to the Hermes tab → expect
  `[hermes] reload -> config generation N` and a visible reload.
- ✋ Ear-tests (see §3) and the PAT revoke (~2 weeks, Debi's calendar).
- ⚠️ Undiagnosed observation for Fable: Hermes **declined to delete** a file it had
  just created (approval flow for creation worked). Upstream posture, our path-guard
  policy, or model caution — nobody has looked yet. Debi is raising it with Fable.

## 1b. NEW — Help surface (Debi request 2026-08-15, 🧠 Fable design lane)
- 🧠 A **Help** entry in the sidebar **under Logs** that renders the explanations
  users have actually needed: chat lanes, the Hermes tools lever (incl. why Hermes's
  own page can look different), toolsets-vs-skills layers, voice controls, models,
  split view, mid-turn cards, and a short "when something looks wrong".
- **Content already written: `docs/USER-EXPLAINERS.md`** — that file is the source
  text; the design work is the surface (sidebar entry, in-panel rendering, whether it
  is searchable, whether sections deep-link from the controls they explain).
- Related, same lane: the first-run walkthrough and USER-GUIDE(-OPUS5) should pull
  from the same file so the three never drift.

## 1c. NEW — the contract gate must be RUNNABLE on Debi's Mac (2026-08-15)
**STATUS: NOTED FOR FABLE, NOT IMPLEMENTED (Debi's instruction 2026-08-15).** No code
was changed; `bridge/requirements.txt` is untouched.

- **The problem:** the pin-bump gate `python -m pytest bridge/contract_tests/` silently
  reported "No module named pytest" during the Hermes v2026.8.13 bump, so **a vendored
  bump shipped with its safety gate skipped**. (Run in the sandbox afterwards it caught
  3 real failures, one behavioural.) Neither the system python nor `data/bridge-venv`
  has pytest, and `data/bridge-venv` has **no pip module either** — `bootstrap.sh:87`
  creates it with `uv venv` (no `--seed`) and installs via `uv pip install`, so the
  obvious `python -m pip install pytest` also fails.
- **Option A (one line):** add `pytest>=8.0` to `bridge/requirements.txt`. Bootstrap
  then installs it everywhere, and it rides into the fat wheelhouse for free because
  `build_app.sh` 4b does `pip download -r bridge/requirements.txt`. Cost: a test
  dependency lives in the runtime requirements file.
- **Option B:** a separate `bridge/dev-requirements.txt`. Cleaner separation; costs a
  second install path that BOTH bootstrap and the fat installer must learn.
- **Option C (orthogonal, arguably the real fix):** make `scripts/ship.sh` — or a new
  `scripts/verify.sh` — RUN the contract suite and **refuse loudly when it cannot**, so
  the gate cannot be silently skipped again regardless of where pytest lives.
- Interim, for an existing install (mirrors bootstrap; `python -m pip` cannot work):
  `source data/bridge-venv/bin/activate && uv pip install pytest && deactivate`

## 2. Fable QA / design gates (mine, next session)
- 🧠 **Debi explicitly asked Fable to review the Hermes auto-reload mechanism
  (2026-08-15, working and verified, but she wants a second opinion on the design):**
  instead of only checking when you switch to the Hermes tab, the shell now polls
  **every 4 seconds while a Hermes pane is actually on screen** and stops the moment
  it isn't (no polling when Hermes is not visible). It reuses the existing generation
  check rather than duplicating logic, and the safety properties hold — bridge
  unreachable → nothing happens, cannot loop, a bridge restart does not trigger a
  spurious reload. Two consequences she wants looked at: (a) with Hermes in **both**
  panes (via a tab drag) **both** reload, and (b) a reload **discards whatever was on
  screen in Hermes's own page** — mid-scroll or a half-typed message there is reset.
  Still uncovered by design: a Hermes dashboard opened in a real browser outside the
  app (the panel copy says so). Judgement calls to weigh: the 4s number, polling while
  the window is minimised/occluded, and polling while that page is in `failedLoads`.
- 🧠 QA the pending tags: toolset lever (upstream-API write path), drag-tab (shell owns
  strip clicks), compact chrome (font stack + scope), starter-fetch alternates.
- 🧠 Chat-column split: review DRAFT-CHAT-SPLIT-ISOLATION.md (recommendation: ChatPane
  componentization, phase 0 = refactor with ONE instance, zero behavior change). The
  draft found one security-grade coupling (approval cards answer the global session)
  that gets fixed in phase 0 regardless of the split.
- 🧠 Hermes max-thinking-time server guard (bridge kills a runaway deliberation, not
  just the UI) — spec then delegate.
- 🧠 ship.sh hardening (longer wait, fail loudly on empty fingerprint) — ops path, mine.
- 🧠 Snapshot vendor-bump procedure (rsync vs --fat rebuild) — decide at PASS 2 QA.

## 3. Voice lane (builder-ready or gated)
- 🔨 Multi-select clarify checkboxes (v2 of ask cards).
- 🔨 Per-skill trimming (skills.disabled) if the one-switch skills lever proves coarse.
- 🔬 silero-VAD upgrade — only if energy VAD false-triggers in practice.
- 🔬 Barge-in for conv mode (needs echo cancellation strategy).
- ⏬ Persistent STT worker (B3) — **DEPRIORITIZED TO LAST, Debi 2026-08-15: "leave the
  STT out for the last last thing on the roadmap as currently it works alright".** No
  measurement needed until then; dictation/conv latency is acceptable in practice. See
  §7 — do not pick this up ahead of anything else.
- ✋ Ear-tests: 575ms hangover (does conv split your sentences?), mlx-audio 0.4.8
  render quality after PASS 1, bf16-vs-fp32 OmniVoice.
- License override table maintenance (hand-kept; next mistagged family needs a row).

## 4. Updates deferred (each needs its own careful pass)
- Odysseus dev bump (+44 commits; 2 contract tests break on moved files; add the
  history limit/offset truncation guard at bump time; NEVER bump toward origin/main).
- VoiceStudio v0.5.0 (install-fragility risk), uv 0.12.4 (trivial), SearXNG pin
  doctrine fix (pin a commit, not `main`).

## 5. Distribution / ops
- Fat dmg rebuild (bakes mic Info.plist + all Swift: split, downloads, drag) → the
  2nd-Mac test that has never happened (§G-phase2 first-run flow still unexercised).
- Panel-triggered "update MLX runtime" (closes the run-from-snapshot ops gap).
- PART 4 update-available notice (old fat-installer item, still open).

## 6. Bigger arcs (original M-plan, still ahead)
- M4 build/test pipeline (flutter build/test, GH Actions, vision-verified builds).
- M5 remote access: Tailscale + the Hermes messaging gateway (Telegram first).
- Parallel-worktree orchestrator (maps onto Hermes delegate_task).
- Fact-check ✓ badge; path-guard coverage for arbitrary shell writes (sandbox-exec).
- ComfyUI (gated on a real model-RAM scheduler); Voicebox deeper integration (its MCP
  toggles stay OFF pending the voice-capability-vs-app decision — mostly superseded by
  native voice).
- Docs refresh: user guides need the newest features (auto/conv chips, clarify cards,
  split view, compact chrome, toolset lever).

## 7. LAST — only when everything above is done
- **Persistent STT worker (B3).** Debi's explicit ordering (2026-08-15): STT works
  well enough today, so this sits at the very bottom. When/if it is picked up, the
  gate still applies first — run the load-once/transcribe-thrice measurement in
  `docs/research/2026-08-14-stt-and-starter-voices.md` §3.4 and only build if
  `import + load` genuinely dominates steady-state inference.
