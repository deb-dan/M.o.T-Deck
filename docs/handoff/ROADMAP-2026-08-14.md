# Harness roadmap — CURRENT STATE (rewritten 2026-08-21, Fable 5)
✋ = Debi action · 🧠 = Fable spec/ruling · 🔨 = builder-ready · 🔬 = research first
History lives in CLAUDE.md. This file: what remains + what awaits verify. 
(Process note: the two prior "updates" to this file silently failed — a text-replace
that matched nothing. Fixed 2026-08-21; edits to this file are now verified by grep.)

## 1. Awaiting Debi verify (shipped)
- LOffice tab — REBUILDING NOW (page was blank on the Mac despite fixes; WKWebView
  diagnosis in flight). Univer assets confirmed reaching the snapshot.
- Appearance v2 — REBUILDING NOW to the Unsloth-literal interaction (floating
  translucent popover, drag-to-reorder, live-apply toggles) after v1's ↑↓-button
  page missed the reference; reorder path being verified end-to-end.
- Aider tab — WORKING (Debi verified a full session; install, PTY, thinking/answer,
  file-create prompt). Slice 2 queued: model handoff + workspace picker.
- Music ✦ Studio view v1.2 — mostly verified; cancel + long-render progress still
  to exercise.
- Sampling floors on Hermes/Agent lanes (set temp 0.2, reload, ask twice).
- ✋ PAT revoke (~2 weeks from 2026-08-20).

## 2. Active queue (in order)
1. 🔨 Appearance v2 (in flight) + LOffice fix (in flight).
2. 🔬 IN FLIGHT: OpenCode (+ OpenClaw / omnigent-ai/omnigent) recon — Debi wants a
   more robust IDE-style coding lane beside aider; goose stays rejected unless the
   recon overturns it.
3. 🔬 IN FLIGHT: SPEECH-TO-SPEECH research — Debi found NVIDIA-NemotronLabs-VoiceChat-11B;
   evaluate the S2S landscape on Apple Silicon (Moshi/moshi-mlx is the known
   full-duplex candidate) for a new instantaneous voice-chat surface (own chat type;
   does NOT replace the TTS/STT lanes).
4. 🔨 Office slice 2: AI side-panel via /api/chat/direct; Docs (.docx) after Sheets
   proves itself. aider slice 2 (model handoff + workspace picker).
5. 🧠 Help surface (sidebar entry under Logs; content ready in docs/USER-EXPLAINERS.md).
6. 🔬 Harness SELF-UPDATE for distributed users (fat-installer update notice first,
   then a pin-disciplined in-app update path).
7. 🔨 Ops follow-up: ship.sh detects bridge/requirements.txt drift vs the snapshot
   venv (openpyxl repeated the pytest class). Music later-asks: mp4/cover export,
   agent-tool exposure ruling.
8. ComfyUI's app-like "Video / Audio" simple tab (two surfaces, one tab) — milestone.

## 3. Done recently (verify complete or in §1)
Sampling v1+v2+v2.1 (incl. MLX 512-token fix, floors, Load group, tooltips/sliders,
lane-byline fix) · Music lane v1/v1.1/v1.2 + Studio deck (dual-engine, cancel, honest
progress, templates, formats) · Studio chrome v1 + form-field wells · ComfyUI +
Unsloth + Music + Aider + LOffice tabs, Swift tab-table generalization (split/drag/
ghosts by construction) · sidebar↔tab navigation · nav model (12-max top, scrollable
sidebar, ⋯ overflow) · ops slice (ship gate via verify.sh, --restart, seed guard,
port preflight refuses foreign listeners) · chatPane Phase 0 (+ cards answer their
own session) · Hermes native browser activated (live-verified) · delete-refusal
closed (model caution) · music measurement (both engines pass) · docs done-pass.

## 4. Standing doctrine (recent additions)
- New tabs inherit split/drag/ghosts/⌘R/lazy/placeholder BY CONSTRUCTION; no
  hardcoded tab counts.
- DESIGN REFERENCES ARE LITERAL (Debi 2026-08-21, after the Appearance v1 miss and
  the first Music deck): when Debi names a reference app/screenshot, match its
  interaction pattern — hover popovers, drag-reorder, live-apply, transparency —
  not an approximation in our own idiom. Recon before design; screenshots are spec.
- Every edit to a doc/file must be VERIFIED to have landed (grep after write) —
  str.replace silently no-ops.
- Pages built from scratch get classic + modern looks where sensible.
- Never use any vendored app's in-app updater; pin bumps only.
- Brace every shell expansion touching non-ASCII; every script 100755 (both fenced
  by test_script_hygiene).
- Additive manifest merge never updates existing snapshot keys — sed step required.

## 5. Watch / parked / rejected (unchanged unless noted)
Watch: Odysseus dev bump (careful pass), VoiceStudio v0.5.0, Hermes > v2026.8.13,
Unsloth stable tag (in-app banner ≠ tags), ComfyUI nightly-torch at each bump,
LICENSE_OVERRIDES upkeep, minimax port updates (revisit for speed).
Parked: persistent STT worker (LAST), Unsloth speed investigation, barge-in/silero,
2nd-Mac dmg test, fat PART 4.
Rejected: video-as-component-now, browser-use, firecrawl, ECC, LangChain,
genoffice/HermesOffice, LibreChat, mindshub, goose*/gemini-cli/OpenHands (*goose
subject to the OpenCode recon's findings), MusicGen, Unsloth-as-wired-component.
