# MOT Deck roadmap — FULL DETAIL (rewritten 2026-08-21, Fable 5; edits verified by grep)
✋ = Debi action · 🧠 = Fable spec/ruling · 🔨 = builder-ready · 🔬 = research first · ⏳ = awaiting verify

## 1. ⏳ Awaiting Debi verify (shipped, not yet confirmed on the Mac)
1. **LOffice** — code proven end-to-end in REAL Chromium in-sandbox (grid, ribbon,
   save); failure is WKWebView/machine-specific. The page now phones home 16 boot
   stages → after opening the tab once: `grep loffice ~/Library/Application\ Support/
   Harness/data/logs/bridge.log` names the failing stage. Build stamp loffice-2026-08-21d.
2. **Floating-surface grammar** — all 8 floating surfaces (Customize, model/audio
   popovers, ⌘K, Logs, install dialog, setup card, peek) share one translucent
   blurred look (alpha .72, blur 20); popover scrolling can no longer close them or
   scroll the page behind (overscroll containment).
3. **View PEEK** — hover a sidebar row (Models/Music/Capabilities) → ⧉ → the view
   opens as a translucent overlay on top of the current page; Esc returns it.
4. **Appearance v2** — drag-to-reorder (⠿), live-apply toggles, floating popover.
   Root cause of v1's "reorder not working": the sidebar draws two groups from one
   list, so cross-group moves saved but didn't show; v2 edits per-group.
5. **OpenCode tab + tools pill** (in flight this wave): component on :4096, pinned
   npm binary, auto-update disabled, tool-calling requirement surfaced; models gain
   a green `tools` pill (chat-template detection, GGUF header reader + MLX config).
6. **Aider tab** — verified working (full session). Music Studio v1.2 cancel +
   long-render progress — still to exercise. Sampling floors on Hermes/Agent lanes
   (temp 0.2 → reload → ask twice). ComfyUI re-verify after start fix (done? confirm).
7. ✋ **PAT revoke** (~2 weeks from 2026-08-20).
8. **MOT rebrand (visible surfaces, 2026-08-21)** — app = **MOT Deck**, Mission
   Control view = **MOT Main**, wordmark = MOT / mixture of tools / local · control;
   OpenCode provider label "MOT Deck (local)". INTERNALS DELIBERATELY UNCHANGED
   (harness.yaml, HARNESS_* env, localStorage harness-* keys, paths,
   /Applications/Harness.app, bundle id) — a full internal rename = a queued ops
   slice with a migration plan (snapshot dir move, localStorage migration, ship.sh
   paths, .app rename). ⚠️ CFBundleName (menu-bar app name) only updates on a full
   build_app.sh rebuild — ship.sh doesn't regenerate Info.plist. ✋ GitHub repo
   rename = Debi's click (Settings → rename; redirects preserve the remote).
9. **LOffice round 3** — AI panel now SHEET-GROUNDED (preamble always sent; Excel
   semantics for function questions; SHEET chip on by default), File ⌄ menu (New/
   Open/Import/Rename/Save/Download/Close/⌂ MOT Main), follows the panel's ◐ theme
   + ▣ chrome via localStorage, rename endpoint. Stamp loffice-2026-08-21i.
   ✋ ONE-TIME: strip a stray NUL byte (tr command in the session note) before ship.

## 2. Active queue (ordered)
1. 🔬→🔨 **S2S voice-chat lane** (Debi GO on direction): measurement first (music-lane
   pattern) — M1 go/no-go: does the 4-bit Qwen3-Omni quant contain the talker weights;
   measure latency/RAM via mlx-vlm's qwen3_omni demo; resolve the NEWEST omni-family
   model with MLX support at build time (text line is at 3.5/3.6/3.8 — the omni line
   lags; pin whatever is newest-with-MLX then). Then Fable specs the "Voice Chat"
   surface: own chat type/button, resident worker (voice-worker pattern, ledger-
   gated ~22GB), additive — conv mode and TTS/STT lanes stay. Moshi = parked
   fallback (200ms full-duplex, weak brain, English-only; pin cc-by-4.0 originals,
   NEVER the -nc derivatives). VoiceChat-11B = watch (clean OpenMDW license; CUDA/
   Linux-only today). Gemma-4-E4B audio-in = cheapest conv-mode latency win (collapses
   STT+LLM) — separate small slice, worth doing regardless.
2. 🔨 **OpenCode follow-through**: VERIFIED WORKING 2026-08-21 (models in the picker,
   provider connected); still queued: the 15-min live sanity vs her biggest coder
   GGUF; aider slice 2 (model handoff + workspace picker).
2b. 🔬→🔨 **Goose PTY lane** (Debi GO 2026-08-21; un-rejects the old verdict):
   goose moved to the Agentic AI Foundation (aaif-goose/goose, Apache-2.0, prebuilt
   darwin-arm64 tarball = the OpenCode installer shape). Tool-calling requirement
   CONFIRMED, no text fallback — acceptable now via the tools pill + start-plan
   warning (the OpenCode precedent). NO first-party browser UI (goosed = client API;
   the :7681 sighting is ttyd) → shape = PTY lane over a generalized pty_aider.py.
   When the VM returns: the 9-item source-verify checklist in
   docs/research/2026-08-21-buzz-goose-recon.md, then build (flip_installed.py +
   exec-bit + origin-gated WS fences apply).
3. 🔨 **Office slice 2**: AI side-panel (selected range + prompt → /api/chat/direct);
   .docx (Docs) after Sheets proves itself; LOG_SOURCES loffice-boot (this wave);
   lazy-load the 11MB engine on first open (queued from forensics).
   - ✅ **PROBE RUN 2026-08-27 (Fable, real-WKWebView instrument): PASS** — ribbon + formula
     calc + high-fidelity xlsx import + docx editor all work; `OO_ISOLATE=1` (COOP/COEP)
     is REQUIRED or the sheet hangs silently at "Loading spreadsheet". Results + recorded
     pins in the runbook's RESULTS section. Adoption slice unblocked; still gating shipping:
     the AGPL ruling, save-back proof, Track-B vendoring, COOP/COEP serving in the bridge.
   - 🔬 **ONLYOFFICE probe kit BUILT 2026-08-21, awaiting Debi's 30-min run** —
     `scripts/probe_onlyoffice.sh` (chmod +x it FIRST) + `docs/handoff/ONLYOFFICE-PROBE-RUNBOOK.md`.
     Two tracks: fernfei/OnlyofficePersonal = the go/no-go in Safari, CryptPad's
     onlyoffice-editor.zip + x2t.zip = the artifacts we'd vendor (hash-checked against
     CryptPad's own installer). PASS → ONLYOFFICE static bundle becomes LOffice tier 2,
     replacing the Univer rich tier, x2t.wasm becomes the converter for both, tier-1 grid
     stays. FAIL → x2t.wasm-as-converter-only. ⚠️ the AGPL-3.0-served-from-our-own-page
     ruling is a SEPARATE Fable call and is not made; a pass means "it works", not "we ship".
4. 🧠 **Help surface** — sidebar entry under Logs; content ready in
   docs/USER-EXPLAINERS.md; should also pull into the first-run tour + user guides.
5. 🔬 **Harness SELF-UPDATE for distributed users** — fat-installer PART 4 (update
   notice) first, then a pin-disciplined in-app update path (Debi: end users must be
   able to update without terminal).
6. 🔨 **Ops follow-ups**: ship.sh detects bridge/requirements.txt drift vs snapshot
   venv (openpyxl repeated the pytest class); prune/rotate loffice-boot-class logs;
   consider LOG_SOURCES completeness test.
7. 🧠 **Chat-split next phases** (Phase 0 done): Phase 2 per-pane localStorage keys,
   Phase 3+ second chat column UI (the original ask) — needs Fable spec per draft.
8. **ComfyUI "Video / Audio" app-like tab** — TWO surfaces under ONE tab (Debi),
   node graph hidden, feeding off headless ComfyUI. Milestone-sized; the Music lane
   may link from it. Optional Kandinsky-5-T2V-Lite MPS spike remains sanctioned.
9. 🧠 **Studio chrome phase 3**: --ui-font-scale as a RATIO (Unsloth pattern);
   consider studio ownership of remaining exempt surfaces case-by-case.
10. **Music later-asks**: mp4/cover-image export (WhatsApp video); agent-tool
    exposure of music generation (deliberate ruling needed); engine speed watch
    (minimax port updates).

## 3. Parked (explicit, with re-entry triggers)
- **Persistent STT worker** — LAST on the roadmap (Debi 2026-08-15); trigger: conv
  latency annoyance returns.
- **Unsloth speed investigation** — same shelf; UX wins already adopted.
- **Barge-in / silero-VAD / keyboard push-to-interrupt** — trigger: false VAD
  triggers or Debi wanting to interrupt TTS playback by voice.
- **Compact chrome** — superseded by Studio chrome (deleted).
- **2nd-Mac dmg test** — optional since first-run provisioning verified in place.
- **omnigent (Databricks meta-harness)** — same product class as the harness; watch
  its POLICIES.md for a generalized path-guard/approvals pattern.
- **M5 remote access** — Tailscale + Hermes messaging gateway (Telegram first);
  OpenClaw's Serve/Funnel model recorded as reference.
- **M5 parallel-worktree orchestrator** — Hermes delegate_task over git worktrees.
- **Fact-check ✓ badge**; **Policies view** (path-guard policy display; nav entry
  removed until it exists); **schema settings form**; **session time-travel**;
  **tiered memory**; **file mailboxes** (all from doc-09, with original triggers).

## 4. Component/update watch
- Odysseus dev bump (+44; 2 contract tests move; history limit/offset caps at 100 —
  add the guard at bump; NEVER bump toward origin/main — it is a stale side branch).
- Hermes: tags > v2026.8.13. Unsloth: stable tag (in-app banner runs ahead of tags;
  never in-app update). VoiceStudio v0.5.0 (install risk). SearXNG pin-a-commit.
- ComfyUI: nightly-torch floater inside the pin — revisit each bump. OpenCode:
  auto-update disabled — bump by pin only. aider upstream vs cecli fork health.
- LICENSE_OVERRIDES is hand-maintained (3 mistag families so far).
- llama.cpp/MLX pins: sweep periodically (last: b10427 / 0.6.13 / 0.4.8).
- MiniMax-Music3 port: revisit for speed/improvements (Debi ask).
- VoiceChat-11B: watch for MLX/GGUF ports. Qwen omni line: newest-with-MLX at S2S
  build time.

## 5. Standing doctrine (operational)
- Ship ONLY via ./scripts/ship.sh (now runs the contract gate and refuses;
  SHIP_SKIP_GATE=1 emergency only). Components stay up: vendored upgrades need
  `./scripts/ship.sh --restart <name>`. Maintenance runs FROM THE SNAPSHOT.
- DESIGN REFERENCES ARE LITERAL (screenshots are spec; match interactions).
- Every doc/file edit verified landed (grep after write).
- New tabs inherit split/drag/ghosts/⌘R/lazy/placeholder by construction; nav
  registry is the single source; 12-max topbar, uncapped scrollable sidebar.
- Floating surfaces share the .float-surface grammar (translucent, blurred,
  overscroll-contained); new popovers/dialogs must join it.
- Pages from scratch: classic + modern looks where sensible.
- Never any vendored app's in-app updater. Pin+bump+contract-test only.
- Scripts: 100755 (fenced), ${var} braced vs multibyte (fenced), zsh-paste-safe,
  no trailing # on runnable lines. Port kills: ownership-checked (foreign → refuse).
- Additive manifest merge never updates existing snapshot keys (sed step).
- Registry user-decisions live in USER_KEYS (voice/ref/hidden/settings/load…).
- RAM ledger gates: runner/aux/voice-worker/music (planning numbers, not RSS).
- Loopback everywhere; WS endpoints origin-gated; path-guard covers Hermes
  write_file/patch only (aider/opencode boundary = their workspace dirs).

## 6. Rejected (do not revisit without NEW facts)
Video-as-component-now (EU license + 152s/5s) · browser-use & firecrawl (Hermes
native browser activated instead — live-verified) · ECC (config bundle) · LangChain
(library) · genoffice/HermesOffice (same repo; Electron; bus-factor 1) · LibreChat
(duplicates Odysseus) · mindshub (rival product class) · OpenClaw (Hermes territory)
· gemini-cli/OpenHands (Gemini-wire-only / 35B+Docker-ish; goose itself UN-REJECTED
2026-08-21 → §2 item 2b, PTY-lane shape) · block/buzz (hive-mind team workspace:
relay needs Postgres+Redis+S3 via Docker Compose — doctrine collision; WATCH: a
Buzz hosted elsewhere is just a URL, and Hermes→Buzz wiring exists upstream)
· chidiwilliams/buzz (native Qt Whisper GUI; our voice lane covers it)
· MusicGen (CC-BY-NC) · Unsloth-as-wired-component
(adopted as arm's-length tab instead) · ONLYOFFICE/Collabora servers (Linux/Docker
-only) · auto-brew / writes outside our tree · Docker anywhere.
