> **DONE — implemented and shipped (marked 2026-08-20).**

# FABLE 5 — UI SPECS (decision-free, for Opus 4.8 implementation) · 2026-07-23

Authority: this file transfers Fable's design lane to Opus for implementation. Every aesthetic decision is made HERE; builders implement without inventing. All work in `bridge/panel/index.html` unless noted; existing `:root` tokens are law; validate (node --check, CSS braces) before ship; each section = its own slice, Mac-verified. Fable QAs everything at project end against the ODDITIES LOG.

## §A — Models pane: two-pane restructure (LM-Studio reference, editorial voice)
Layout: `#view-models` content under the stamp becomes `#models-split { display:flex; gap:26px; }`.
- LEFT `#models-listcol` (flex:1 1 55%; min-width:380px): the installed-model rows. Row anatomy (replaces current .mrow content): line1 = serif name 15px cream; line2 = badge row of mono 9px pills, letter-spacing .12em, uppercase, 2px 7px padding, 6px radius: `[vision]` (border+text var(--ok)) when vision; `[QUANT]` parsed from id by regex `(IQ\d_\w+|Q\d_K_\w+|Q\d_0|\dbit)` (border var(--line2), text var(--dim)); `[gguf|mlx]` (text var(--faint)); size in GB (plain text, not pill); `[live]` existing gold pill; `[aux]` existing. NO buttons in rows. Row states: hover bg var(--card); selected = bg var(--card) + 2px gold left border (mirror .cs-item.on).
- RIGHT `#models-detail` (flex:1 1 45%; position:sticky; top:12px; align-self:flex-start; max-height:calc(100vh - 160px); overflow-y:auto; border:1px solid var(--line); border-radius:14px; padding:22px 24px): renders the SELECTED row. Anatomy: serif title 20px cream; meta line (mono 10.5px dim): id · engine · size · ctx (if known) · source; actions row = existing buttons relocated (Switch/Load/Eject primary-styled as now; Set aux secondary); divider; then for installed models: path (mono 10px faint, word-break); for HF search results (same panel reused): Model card (mdCard render) + Files & fit list with Get buttons (existing functions — move, don't rewrite). Empty state: cs-empty "Select a model".
- Aux runner row stays as a full-width strip UNDER the split (unchanged behavior).
- Search/Download relocation: a chip-style button top-right of the pane header row: `SEARCH / DOWNLOAD ⌄` (style = .chip). Clicking toggles `#models-browse` (the existing browser) shown FULL-WIDTH replacing #models-split, with a `← Library` chip to return. Downloads section (#downloads-sec) visible in BOTH views, above the browse box / below the split.
- JS: keep every existing function; add `selectedModelId` state + `renderDetail()`; hfSearch results render as left-list-style rows too when in browse view, feeding the same detail panel.

## §B — Thought-thread telemetry (chat pane; elevates the transparency contract)
Replace per-tool `.toolline`s with ONE status thread per assistant message: `#`-less div `.statusline` (mono 11px, letter-spacing .06em, color var(--dim), 4px 0 4px 20px): shows the CURRENT state, updating in place: `▸ searching the web…` (web tool start) / `▸ running <tool>…` / `✓ <tool>` on completion (brief, then appended to history line) / `▸ reading results…` (between tool done and next delta). Completed steps collapse into a single summary line: `✓ search · ✓ fetch — 2 steps` once the answer starts streaming. Below it, a `details.inspect` (styled exactly like details.think but summary text "inspect") accumulating raw SSE event JSON, one per line, mono 10px faint, max-height 220px scroll. Glyphs ▸ ✓ only — no emoji. All colors from tokens (gold for the ▸ glyph, ok-green for ✓).

## §C — Theme system + LIGHT theme (dark editorial stays DEFAULT forever)
Mechanism: move current `:root` values to `:root` (unchanged, = dark default); add `html[data-theme="light"] { …overrides… }`. Toggle: a `.chip` in the topbar: `◐ THEME`; cycles dark→light; persisted `localStorage['harness-theme']`; applied on load before first paint (inline script in <head> reading localStorage → set attribute).
LIGHT PALETTE (Fable-decided; warm paper, same editorial DNA — never pure white/black):
--bg:#faf7f0; --bg2:#f2eee3; --card:#ffffff; --card2:#faf6ec; --line:#e6dfcf; --line2:#d6ccb4; --cream:#221d14 (display ink); --fg:#3d372b; --dim:#7c7565; --faint:#a89f8b; --gold:#9c7327; --ok:#177e5d; --warn:#a06f22; --bad:#b13d3d; --accent:#5560c8.
Additional light-only fixes: user chat bubble bg var(--card2); dl-bar track var(--line); ensure .mode-chip.on text stays #171420 on gold (works both themes). Swift shell: leave darkAqua (chrome stays dark — intentional contrast frame; note this in doc). Verify contrast: cream-ink on bg ≥ 12:1, dim ≥ 4.5:1 (these values pass).

## §D — Chat pass-2 polish (function stays; feel refined)
Session rail: active row name → cream 13.5px; add 2px more row padding. Assistant body: max-width 68ch. User bubble: padding 15px 19px. Send button hover: subtle gold border. Mode note: fades (opacity .75). Thinking disclosure summary: add elapsed seconds when >2s: `thinking · 4.2s` (timestamp captured at first thinking delta → answer start). Nothing else moves.

## §E — First-launch checklist + walkthrough (OpenHuman-inspired mechanics, editorial skin)
Checklist: on panel load, if `localStorage['harness-setup-done']` absent AND any component uninstalled/stopped → overlay card (dialog-style, existing dialog tokens) "Setting up" listing: Bridge ✓ · Runner · Hermes · Odysseus · SearXNG — each row: name (serif 15px) + state glyph (✓ ok-green / ▸ starting gold / ○ faint) + a Start/Install button (reuses act()/planInstall()). Footer: "Run in background" (dismiss, sets flag when all green) + never blocks the panel (overlay closable always). Walkthrough: after checklist dismissed first time, 4 coach-cards (position:fixed near targets, dialog tokens, 300px wide): 1 Chat nav ("Talk to the stack — Agent uses tools, Chat is direct"), 2 Models nav ("Library, downloads, RAM ledger"), 3 Browse chip ("Give agents your browser"), 4 component cards ("Start/stop everything; green = healthy"). Next/Skip; `localStorage['harness-tour-done']`.

## §F — File/artifact cards (chat) — SPEC ONLY, build LAST, ⚠️ security QA required
When a future attach/file feature lands: message file references render as cards: 34px doc glyph (◳), name (13px cream), meta (mono 10px faint: ext · size), action chip "Open ↗" → extend /api/open to allow `file://` ONLY under $HOME with an allowlist check + `open -R` reveal option. Do NOT extend /api/open before Fable QAs the path-allowlist logic (tag ⚠️).

## §G — Distribution: .dmg + portability (Debi wants to test on another Mac)
Phase 1 (NOW): verify `./scripts/build_app.sh --dmg` produces `dist/Harness.dmg` (script claims support; test, fix if broken — hdiutil create -volname Harness -srcfolder dist/Harness.app -ov -format UDZO dist/Harness.dmg). Prereq on any Mac: Xcode Command Line Tools (`xcode-select --install`). Document the one command in CLAUDE.md.
HONEST LIMIT (tell Debi): the .app currently hardcodes harnessRoot at build time and requires the repo + venvs on disk — the dmg alone is NOT portable to a fresh Mac yet.
Phase 2 (next milestone, "PORTABLE FIRST-RUN"): build_app.sh gains `--portable`: bundles a source snapshot (tar of repo minus data/vendor/dist/.git) into Harness.app/Contents/Resources/harness-seed.tar.gz; main.swift first-run (no harnessRoot found): dialog → extract seed to ~/Harness → run a new scripts/firstrun.sh (installs uv via curl astral.sh if missing; checks git, python3, node with actionable messages; bootstrap.sh --yes; then opens the panel where the §E checklist guides component installs). Ship §E BEFORE Phase 2 so the experience exists. Requirements honesty: fresh Mac still needs Xcode CLT + network; document.

## Execution order for Opus: §G-phase1 → §A → §B → §C → §D → §E → §G-phase2 → (§F last, gated).
