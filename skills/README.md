# MOT Deck skills

Git-versioned Hermes skills. Synced with `~/.hermes/skills` so skills the agent
creates survive updates and machine moves — `git log` here is the agent's visible
learning history.

Planned MOT Deck-authored skills (M4): build-flutter, update-motdeck, verify-visual.
